# focus window
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east

# swap managed window
#shift + alt - h : yabai -m window --swap north

# move managed window
shift + alt - h : yabai -m window --warp west
shift + alt - j : yabai -m window --warp south
shift + alt - k : yabai -m window --warp north
shift + alt - l : yabai -m window --warp east

# fast focus desktop
ctrl + shift + alt - k : yabai -m space --focus recent
alt - 1 : yabai -m space --focus 1
alt - 2 : yabai -m space --focus 2
alt - 3 : yabai -m space --focus 3
alt - 4 : yabai -m space --focus 4
alt - 5 : yabai -m space --focus 5
alt - 6 : yabai -m space --focus 6
alt - 7 : yabai -m space --focus 7
alt - 8 : yabai -m space --focus 8
alt - 9 : yabai -m space --focus 9

# send window to desktop and follow focus
shift + alt - 1 : yabai -m window --space 1; yabai -m space --focus 1
shift + alt - 2 : yabai -m window --space 2; yabai -m space --focus 2
shift + alt - 3 : yabai -m window --space 3; yabai -m space --focus 3
shift + alt - 4 : yabai -m window --space 4; yabai -m space --focus 4
shift + alt - 5 : yabai -m window --space 5; yabai -m space --focus 5
shift + alt - 6 : yabai -m window --space 6; yabai -m space --focus 6
shift + alt - 7 : yabai -m window --space 7; yabai -m space --focus 7
shift + alt - 8 : yabai -m window --space 8; yabai -m space --focus 8
shift + alt - 9 : yabai -m window --space 9; yabai -m space --focus 9

# increase window size
#shift + alt - h : yabai -m window --resize left:-20:0
#shift + alt - k : yabai -m window --resize top:0:-20

# decrease window size
shift + cmd - s : yabai -m window --resize bottom:0:-20
shift + cmd - w : yabai -m window --resize top:0:20

# set insertion point in focused container
# ctrl + alt - h : yabai -m window --insert west

# toggle window zoom
alt - f : yabai -m window --toggle zoom-fullscreen
shift + alt - f : yabai -m window --toggle native-fullscreen

# toggle window split type
#alt - e : yabai -m window --toggle split

# float / unfloat window and center on screen
alt - space : yabai -m window --toggle float --grid 4:4:1:1:2:2

# launch apps
alt - e : open -a Finder
alt - w : open '/Applications/Brave Browser.app'
#ctrl + shift + alt - i : open '/Applications/Firefox.app'
#ctrl + shift + alt - o : open '/Applications/LibreOffice.app'
#ctrl + shift + alt - l : open '/Applications/Zathura.app'
#ctrl + shift + alt - e : open '/Applications/Emacs.app'
#ctrl + shift + alt - g : open '/Applications/Ghostty.app'
alt - t : open '/Applications/Telegram.app'
alt - return : open '/Applications/kitty.app'
alt - d : open '/Applications/Sol.app'

ctrl + alt - r : yabai --restart-service

#ctrl + shift + alt - p : /bin/zsh -lc 'rmpc togglepause'

# TODO modal

:: winmode
:: default
#meh - s ; winmode
#winmode < escape ; default
#winmode < meh - s ; default
#winmode < h : yabai -m window --focus west || yabai -m display --focus west
#

#cmd - d : kitten quick-access-terminal --config ~/.config/kitty/quick-access-terminal.conf --instance-group app-selector /bin/bash -c '$HOME/.config/scripts/launcher.sh'

