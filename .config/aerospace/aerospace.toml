after-login-command = []

#after-startup-command = [
#  'exec-and-forget sketchybar'
#]

#exec-on-workspace-change = [
#  '/bin/bash',
#  '-c',
#  'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE',
#]

start-at-login = false
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true
accordion-padding = 10

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

automatically-unhide-macos-hidden-apps = false

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

[workspace-to-monitor-force-assignment]
    "[7]" = 'secondary'
    "[8]" = 'secondary'
    "[9]" = 'secondary'

[gaps]
    inner.horizontal = 0
    inner.vertical =   0
    outer.left =       0
    outer.bottom =     0
#    outer.top =        [{ monitor."built-in retina display" = 5 }, 0]
    outer.top =        0
#    outer.top =        [{ monitor."built-in retina display" = 15 }, 47]
    outer.right =      0

[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    alt-slash = 'layout tiles horizontal vertical'
    alt-comma = 'layout accordion horizontal vertical'

    alt-h = 'focus left'
    alt-j = 'focus down'
    alt-k = 'focus up'
    alt-l = 'focus right'

    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l = 'move right'

    alt-f = 'fullscreen'
    alt-shift-f = 'macos-native-fullscreen'

    alt-minus = 'resize smart -50'
    alt-equal = 'resize smart +50'

    alt-1 = 'workspace [1]' # terminal
    alt-2 = 'workspace [2]' # browser
    alt-3 = 'workspace [3]' # emacs
    alt-4 = 'workspace [4]' # youtube/music
    alt-5 = 'workspace [5]' # social
    alt-6 = 'workspace [6]'
    alt-7 = 'workspace [7]'
    alt-8 = 'workspace [8]'
    alt-9 = 'workspace [9]'

    alt-shift-1 = 'move-node-to-workspace [1]'
    alt-shift-2 = 'move-node-to-workspace [2]'
    alt-shift-3 = 'move-node-to-workspace [3]'
    alt-shift-4 = 'move-node-to-workspace [4]'
    alt-shift-5 = 'move-node-to-workspace [5]'
    alt-shift-6 = 'move-node-to-workspace [6]'
    alt-shift-7 = 'move-node-to-workspace [7]'
    alt-shift-8 = 'move-node-to-workspace [8]'
    alt-shift-9 = 'move-node-to-workspace [9]'

# launch apps
    alt-enter = 'exec-and-forget open -a kitty'
    alt-w     = 'exec-and-forget open -a Brave\ Browser'
    alt-y     = 'exec-and-forget open -a Yandex'
    alt-m     = 'exec-and-forget open -a Yandex\ Music'
    alt-e     = 'exec-and-forget open -a Emacs'
    alt-d     = 'exec-and-forget open -a KeePassXC'
    alt-q     = 'exec-and-forget open -a Finder'
    alt-a     = 'exec-and-forget open -a Launchpad'

    alt-tab = 'workspace-back-and-forth'
    alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

    alt-shift-semicolon = 'mode service'

    alt-space = 'layout floating tiling'

# 'service' binding mode declaration.
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    backspace = ['close-all-windows-but-current', 'mode main']

    alt-shift-h = ['join-with left', 'mode main']
    alt-shift-j = ['join-with down', 'mode main']
    alt-shift-k = ['join-with up', 'mode main']
    alt-shift-l = ['join-with right', 'mode main']

    down = 'volume down'
    up = 'volume up'
    shift-down = ['volume set 0', 'mode main']

# Finder
[[on-window-detected]]
    if.app-id = 'com.apple.finder'
    run = 'layout floating'

# AmneziaVPN
[[on-window-detected]]
    if.app-id = 'e.AmneziaVPN'
    run = 'layout floating'

# Ghostty
[[on-window-detected]]
    if.app-id = "com.mitchellh.ghostty"
    run = "move-node-to-workspace [1]"

# Kitty
[[on-window-detected]]
    if.app-id = "net.kovidgoyal.kitty"
    run = "move-node-to-workspace [1]"

# Browser-Safari
[[on-window-detected]]
    if.app-id = "com.apple.Safari"
    run = "move-node-to-workspace [2]"

# Browser-Brave
[[on-window-detected]]
    if.app-id = "com.brave.Browser"
    run = "move-node-to-workspace [2]"

# Yandex-Music
[[on-window-detected]]
    if.app-id = "com.apple.Safari.WebApp.D6D05ACC-D3BF-473A-9CCA-C46FF02F8A9D"
    run = ["layout floating", "move-node-to-workspace [4]"]

# Emacs
[[on-window-detected]]
    if.app-id = "org.gnu.Emacs"
    run = "move-node-to-workspace [1]"

# Telegram
[[on-window-detected]]
    if.app-id = "ru.keepcoder.Telegram"
    run = ["layout floating", "move-node-to-workspace [5]"]

# Signal
[[on-window-detected]]
    if.app-id = "org.whisperrystems.signal-desktop"
    run = ["layout floating", "move-node-to-workspace [5]"]

# KeePassXC
[[on-window-detected]]
    if.app-id = "org.keepassxc.keepassxc"
    run = ["layout floating"]


